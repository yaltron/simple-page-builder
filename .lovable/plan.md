Remove the "Request Callback" button from the doctor cards in `src/components/doctors-carousel.tsx`, leaving only "Consult Now" and "View Profile". Adjust the button row layout so the two remaining buttons fill the space cleanly (no leftover gap from the third button).

No other files affected.